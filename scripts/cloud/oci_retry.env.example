# OCI A1 retry launcher environment (copy to scripts/cloud/oci_retry.env)
# 주의: 이 파일에는 민감 정보(OCID, Webhook URL)가 포함될 수 있으므로
# 실제 파일은 git에 커밋하지 말고 chmod 600으로 권한을 제한하세요.

OCI_PROFILE=DEFAULT

# 필수
COMPARTMENT_OCID=ocid1.compartment.oc1..replace_me
SUBNET_OCID=ocid1.subnet.oc1.ap-chuncheon-1.replace_me
AVAILABILITY_DOMAIN=UBJq:AP-CHUNCHEON-1-AD-1

# 선택: 이미지 자동 탐색을 위해 tenancy(root) OCID 권장
IMAGE_COMPARTMENT_OCID=ocid1.tenancy.oc1..replace_me
# IMAGE_OCID=ocid1.image.oc1.ap-chuncheon-1.replace_me

# 인스턴스 사양(요청값)
INSTANCE_NAME=coinpilot-ins
SHAPE=VM.Standard.A1.Flex
OCPUS=2
MEMORY_GBS=12
BOOT_VOLUME_SIZE_GBS=50
ASSIGN_PUBLIC_IP=true

# 재시도 정책
RETRY_INTERVAL_SECONDS=600
MAX_ATTEMPTS=0
THROTTLE_RETRY_BASE_SECONDS=900
THROTTLE_RETRY_MAX_SECONDS=3600
THROTTLE_JITTER_MAX_SECONDS=120

# 선택: Discord 알림
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxx/yyy
# DISCORD_NOTIFY_EVERY_N_ATTEMPTS=10
